#import "template.typ":*
#import "@preview/codelst:1.0.0": sourcecode

#let codelst-sourcecode = sourcecode
#let sourcecode = codelst-sourcecode.with(
frame: block.with(
fill: none,
stroke: none,
radius: 2pt,
inset: (x: 10pt, y: 5pt)
)
)

#show: project.with(
  title: [
    Курсова Робота\
    з дисципліни\
    Паралельні обчислення
  ],
  author: "Зарицький Кирило Андрійович"
)

#outline(
  title: [Зміст]
)

#show heading.where(
  level: 1
): it => [
  #pagebreak(weak: true)
  #it.body
]

#pagebreak()
= Вступ

#[



]

= Теоретичні Відомості

#[



]

= Модель Системи

#[



]

= Опис Реалізації

#[

В цьому розділі буде розглянуто реалізацію моделі системи. Далі буде розглянуто структуру проекту, опис його модулів та синхронізації в цих модулях. Також в кінці розділу є інструкція з інсталювання.

Загалом проект був розроблений на мові програмування C++ для Unix подібних систем (в першу чергу Linux).

== Структура Проекту

Побудова, інсталювання та тестування проекту виконано за допомогою системи CMake. Її конфігураційні файли розташовані на 

== Опис Модулів



== Опис синхронізації



== Керівництво з Встановлення Програми


  
]

= Виконання програми

#[

]

= Висновки

#[

]


